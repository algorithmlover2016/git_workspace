command git 
git init
git add filename
git commit
git commit -m "comment to describe the file function or something about it"
git checkout -- <file>... to discard changes in working directory
git status
git diff HEAD -- filename
git reset HEAD <file>... used to unstage
git log --pretty=oneline # back_ward oldest

back to history version
git reset --hard HEAD  # commit_id
git reset --hard HEAD^
git reset --hard HEAD^^
git HEAD~4
git reflog # forward lastest

difference bewteen checkout and reset
git checkout -- filename # recovery filename from master
git reset HEAD filename # unstage filename after add but not commit

string no use
git is a distributed version control system
git is a free software distributed under the GPL.
